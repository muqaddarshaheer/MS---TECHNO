# Local (Vite proxies /api to backend)
# Leave empty or omit for local: uses /api via vite proxy

# Production (Vercel) — set in Vercel Project Settings → Environment Variables
# Example: https://ms-techno-api.up.railway.app/api
VITE_API_URL=
